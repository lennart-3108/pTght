# HTTP-Server Port
PORT=5001

# SQLite (relative to backend folder)
DB_PATH=sportplattform.db

# CORS
CORS_ORIGIN=http://localhost:3000

# JWT

# --- AI Test Agent (optional) ---
# ENABLE_INTERNAL_AI_AGENT=1
# AGENT_API_BASE=http://127.0.0.1:5001/api
# AGENT_TOKEN=BearerTokenIfNeeded
# AGENT_COMMANDS=npm run check:all;npm run check:homepage-quality;npm --prefix ../frontend run build
# AGENT_INTERVAL_MS=300000
# AGENT_SKIP_BOOT_RUN=0
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini