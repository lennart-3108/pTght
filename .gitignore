# Node
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build artefacts
frontend/build/
frontend/.cache/
dist/
coverage/

# Env/Secrets
*.env
*.env.local
frontend/.env
frontend/.env.local
backend/.env
backend/.env.local

# Editor/OS
.DS_Store
.vscode/
.idea/

# VSCode/JS project config
jsconfig.json

# Autocomplete helper/generated files
autocomplete/*

# Fehlerhafte Datei im Frontend (sollte nicht getrackt werden)
frontend/src/dev.matchleague.org {

# Misc
*.local

# SQLite DB and backups
backend/sportplattform.db
backend/*.sqlite
backend/*.db
backend/*.db-*

# Top-level SQLite DBs (should not be committed)
/*.sqlite
/*.db
/*.db-*

# Local backup folder (moved pTght-1 to Desktop previously)
/pTght-1/

# Mailer artifacts
*.eml

# Python backend
backend/__pycache__/
backend/*.py[cod]
backend/.venv/
backend/venv/
backend/.pytest_cache/
backend/.mypy_cache/
backend/.ruff_cache/
backend/.coverage*
backend/.tox/

# Logs and temp
logs/
tmp/
backend/.env.prod
/backend/.env

# Copilot temporary SSH keys (do not commit)
.copilot_strato_key
.copilot_strato_key.pub
# Local runtime artifacts (do not commit)
backend/backend-run.log
frontend-run.log
frontend/frontend.pid

# Local npm cache at repo root (avoid tracking thousands of cache files)
/.npm-cache/
*.db.before_cleanup_*
*.pid
*-start.log
backend/sportsplatform.db.before_cleanup_379k
backend-dev.log
backend/.env.test
